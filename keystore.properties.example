# Copy to keystore.properties (gitignored) or set the same keys via RELEASE_* environment variables.
# Paths can be absolute or relative to the project root.

storeFile=C:\\Users\\Charles\\Key.jks
storePassword=
keyAlias=
keyPassword=
